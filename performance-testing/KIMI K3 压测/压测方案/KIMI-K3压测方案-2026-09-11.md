# AI 运营平台 KIMI K3 API 性能压测方案

> 文档日期：2026-09-11  
> 当前状态：单轮正式方案已定稿，尚未启动模型调用和性能压测  
> 测试环境：`198.51.100.10`  
> 压测工具：Apache JMeter 5.6.3，Java 17  
> 唯一测试模型：**KIMI K3(压测)**  
> API 请求中必须使用的精确模型值：`kimi-k3(压测)`

## 1. 测试结论先行

本次不通过浏览器反复操作“模型体验”页面发压，而是使用 JMeter 直接调用平台对外提供的 OpenAI 兼容接口：

```http
POST http://198.51.100.10/v1/chat/completions
```

该方式会真实经过以下完整链路：

```text
JMeter
  -> AI 运营平台API网关
  -> API Key鉴权
  -> 模型权限、额度和限流校验
  -> 固定模型路由
  -> KIMI K3(压测)对应供应商
  -> 大模型推理
  -> 平台响应处理
  -> 用量、计费和调用日志记录
  -> JMeter
```

因此它最适合同时评估：

1. AI 运营平台的API网关、鉴权、路由、连接池、日志和计费链路负载。
2. `KIMI K3(压测)`背后供应商的并发承载、排队、限流和推理性能。
3. 整条端到端链路在最高400并发线程下的可用性、吞吐量和响应时间。

本方案禁止使用以下模型：

- `自动路由`
- `deepseek-v4-pro-qinwenxiu`
- 任何其他模型

原因：自动路由可能把请求分配至不同供应商，无法准确评估KIMI K3供应商负载；其他模型不在本次任务范围内。

## 2. 已核实的环境信息

### 2.1 平台信息

| 项目 | 已核实内容 |
|---|---|
| 平台页面 | `https://198.51.100.10` |
| API Base URL | `http://198.51.100.10/v1` |
| 模型列表接口 | `GET /v1/models` |
| 文本生成接口 | `POST /v1/chat/completions` |
| 鉴权方式 | `Authorization: Bearer <API_KEY>` |
| API协议 | OpenAI兼容API |
| 目标模型展示名 | `KIMI K3(压测)` |
| API实际模型值 | `kimi-k3(压测)` |

已使用API Key对`GET http://198.51.100.10/v1/models`做过一次只读验证，接口返回的目标模型精确值为：

```text
kimi-k3(压测)
```

JMeter Body 中必须原样使用这个值，不应自行改成全大写，也不应增加空格。

### 2.2 压测机信息

| 项目 | 当前信息 |
|---|---|
| JMeter | Apache JMeter 5.6.3 |
| Java | Liberica JDK 17.0.19 LTS |
| CPU | Intel i5-13500H，12核/16逻辑处理器 |
| 内存 | 约15.73 GB |
| JMeter默认堆 | `-Xms1g -Xmx1g` |

工具版本能够支持本次最高400线程测试，但正式执行必须采用非GUI模式。建议执行前关闭浏览器多余标签、IDE、办公软件等高内存程序，使可用内存达到5 GB以上，并将JMeter堆调整为：

```powershell
$env:HEAP = "-Xms1g -Xmx2g -XX:MaxMetaspaceSize=256m"
```

## 3. 测试目标

### 3.1 核心目标

1. 获取1线程条件下的接口功能和性能基线。
2. 验证25、50、100、200、400并发线程下端到端调用是否稳定。
3. 确认各并发阶段的成功率、错误率、吞吐量、P90、P95、P99和最大响应时间。
4. 找出响应时间明显增长、错误率开始上升或吞吐量不再增长的性能拐点。
5. 确认首次出现429、平台5xx、供应商5xx、连接重置和读取超时的并发级别。
6. 判断400并发下平台是否仍能完成API Key鉴权、固定模型路由、供应商调用、计费和日志落库。
7. 结合服务端监控区分平台瓶颈与KIMI K3供应商瓶颈。
8. 验证高压结束后，接口是否在合理时间内恢复到基线水平。

### 3.2 本次不测试

- 登录接口和验证码性能。
- 浏览器页面渲染性能。
- 模型体验页面的前端交互性能。
- 自动路由性能。
- 其他模型或多模型混合流量。
- 多轮上下文对话。
- 图片、语音、视频和Embedding接口。
- 供应商直连性能，除非后续由开发或运维提供供应商直连接口和测试Key。

## 4. 统一测试数据与请求口径

### 4.1 固定问题

所有正式请求统一使用：

```text
给出一套成都旅游一日游方案，50字左右
```

不得随机替换问题，不得携带上一轮问答，也不得在同一个上下文中累计消息。

### 4.2 推荐正式请求

```http
POST http://198.51.100.10/v1/chat/completions
Authorization: Bearer sk-REPLACE_WITH_YOUR_KEY
Content-Type: application/json
Accept: application/json
```

非流式请求Body：

```json
{
  "model": "kimi-k3(压测)",
  "messages": [
    {
      "role": "user",
      "content": "给出一套成都旅游一日游方案，50字左右"
    }
  ],
  "temperature": 0.2,
  "max_tokens": 100,
  "stream": false
}
```

参数固定原则：

| 参数 | 固定值 | 目的 |
|---|---:|---|
| `model` | `kimi-k3(压测)` | 保证所有请求落到指定模型和供应商 |
| `temperature` | `0.2` | 降低随机性，使各阶段输出相对可比 |
| `max_tokens` | `100` | 控制最大输出规模，减少输出长度波动 |
| `stream` | 首轮`false` | 便于JMeter准确完成JSON断言和总耗时统计 |

注意：自然语言中的“50字左右”不能严格控制输出Token数，所以必须同时固定`max_tokens`。正式压测前应以单请求确认100 Token不会把正常答案截断。如果目标模型不接受`temperature`或`max_tokens`，应以单请求返回为依据修改，但五个梯度阶段必须保持完全相同的Body。

### 4.3 会话隔离

对外OpenAI兼容接口的主请求不需要登录Token，也不使用平台页面的登录会话。每次请求只包含一个`user`消息，因此天然是独立单轮请求。

不得在每个JMeter线程内执行登录和验证码请求。否则测到的将是登录系统加模型API的混合性能，并会造成验证码大量刷新，污染结论。

## 5. 为什么主测试采用API Key直调

平台模型体验页面内部使用登录会话，并调用：

```http
POST /api/v1/chat/completions
Accept: text/event-stream
```

模型体验页面适合验证页面功能，但不是本次负载测试主入口，原因如下：

1. 页面调用会混入登录Token、页面逻辑、前端渲染和浏览器资源消耗。
2. 400个浏览器会话会先压垮压测机，不能代表API服务真实容量。
3. 用户实际要验证的是平台API Key调用链路。
4. `POST /v1/chat/completions`能稳定固定`model=kimi-k3(压测)`。
5. JMeter可以直接对API采集状态码、吞吐量、分位响应时间和错误类型。

## 6. 整体执行顺序

正式测试只执行**一轮完整流程**，不再安排第二轮扩大循环复测。该轮分为四部分，必须按顺序执行：

```text
T0：环境和单请求预检
  -> T1：非流式五级折中梯度容量测试
  -> T2：流式三档代表性连接专项测试
  -> T3：恢复性验证
```

本轮正式压力请求总数为：

```text
非流式29,500次 + 流式6,250次 = 35,750次
```

另有T0预检3次和T3恢复检查10次，因此整个执行流程预计产生35,763次模型调用。每个阶段必须使用独立Label、JTL和HTML报告，最终再汇总为同一份桌面Markdown测试报告。

任何阶段达到停止条件，都必须停止继续升压，先保存结果并排查。

## 7. T0：压测前预检

T0不是性能结论，只用于证明脚本和业务链路配置正确。

### 7.1 API Key和模型检查

请求：

```http
GET http://198.51.100.10/v1/models
```

通过条件：

- HTTP状态码为200。
- 响应模型列表包含精确值`kimi-k3(压测)`。
- 不出现401、403、429或额度不足错误。

### 7.2 单请求功能检查

配置：

```text
线程数：1
Ramp-up：1秒
循环次数：3
stream：false
```

必须人工检查至少一次完整响应，确认：

- HTTP 200。
- 响应是JSON。
- `choices[0].message.content`非空。
- 回答内容与成都一日游有关。
- 回答没有明显截断。
- 如果响应包含`usage`，则`total_tokens > 0`。
- 平台消费记录或调用日志能够查询到对应请求。

只有T0连续3次全部成功，才允许进入25线程阶段。

## 8. T1：非流式五级梯度容量测试

### 8.1 推荐梯度

| 阶段 | 并发线程 | 每线程循环 | 预计请求总数 | Ramp-up | 阶段间休息 |
|---|---:|---:|---:|---:|---:|
| P1 | 25 | 20 | 500 | 30秒 | 3分钟 |
| P2 | 50 | 20 | 1,000 | 60秒 | 3分钟 |
| P3 | 100 | 20 | 2,000 | 120秒 | 3至5分钟 |
| P4 | 200 | 30 | 6,000 | 180秒 | 5分钟 |
| P5 | 400 | 50 | 20,000 | 300秒 | 5分钟后进入流式专项 |

正式五档共计：

```text
500 + 1,000 + 2,000 + 6,000 + 20,000 = 29,500次模型请求
```

首轮不使用每线程500次。若按五档均循环500次，总调用量会达到几十万次，测试时长、供应商消耗和日志量过大，也不利于快速发现拐点。

### 8.2 为什么采用20、20、20、30、50次折中循环

- 25至100线程使用20次循环，兼顾样本量和低压阶段执行时间。
- 200线程提高到30次循环，获得6,000个中高压样本。
- 400线程提高到50次循环，获得20,000个最高压样本，用一轮同时观察容量和持续稳定性。
- 正式JTL不保存完整回答，所以样本量增加后主要增加测试时长和服务端负载，不应显著扩大JMeter内存占用。
- 本轮结束后不再安排相同梯度的第二轮50循环复测。

循环次数主要影响持续时间和样本数；线程数才直接决定瞬时并发压力。因此不能把“本机能开400线程”理解为“每个线程必须循环500次”。

正式执行前已重新核实本机资源；使用非GUI模式与`-Xmx2g`，并由脚本持续采集CPU和可用内存，达到停止闸门时不再继续升压。

### 8.3 400线程不等于400 RPS

JMeter标准线程组属于闭环模型：一个线程必须等上一次模型回答完成后才发送下一次请求。

估算关系：

```text
吞吐量（请求/秒）约等于 并发线程数 / 平均完整响应时间（秒）
```

例如400线程、平均响应时间20秒：

```text
400 / 20 = 约20 RPS
```

所以本测试声明的是“最高400并发线程”，不能在报告中写成“400 RPS”，除非JMeter实际吞吐量确实达到400次/秒。

## 9. T2：流式连接专项测试

非流式五档结束并确定系统稳定区间后，再执行流式专项。流式请求Body仅修改：

```json
"stream": true
```

Header改为：

```http
Accept: text/event-stream
```

流式专项仍属于同一轮完整执行，但只选择25、200、400三个有代表性的并发档，每线程10次：

| 阶段 | 并发线程 | 每线程循环 | 请求总数 | Ramp-up |
|---|---:|---:|---:|---:|
| S1 | 25 | 10 | 250 | 30秒 |
| S2 | 200 | 10 | 2,000 | 180秒 |
| S3 | 400 | 10 | 4,000 | 300秒 |

流式专项共计：

```text
250 + 2,000 + 4,000 = 6,250次模型请求
```

流式阶段没有采用50循环，是因为其目标是验证SSE长连接、首Token、持续输出和断流情况；最高档4,000个流式样本已足以做首轮判断，同时避免整个单轮方案膨胀到接近60,000次调用。

流式专项关注：

- HTTP响应是否为`text/event-stream`。
- SSE数据是否持续返回。
- 是否以`data: [DONE]`正常结束。
- 流中是否出现业务`error`。
- 中途断流率。
- 连接重置、读取超时和网关主动断开数量。
- 平台活动连接数与上游连接池占用。
- 供应商首Token时间和完整生成时间。

重要限制：JMeter普通HTTP Request记录的`Elapsed`是完整SSE流结束时间，`Latency`也不能直接等同于大模型首Token时间TTFT。若验收要求TTFT，优先从平台服务端日志采集`upstream_first_token - upstream_start`，或者增加专门的SSE分块计时脚本。

## 10. T3：恢复性验证

400线程阶段结束后等待2分钟，再执行：

```text
线程数：1
循环次数：10
Ramp-up：1秒
stream：false
```

恢复通过条件：

- 10次请求全部成功。
- 不再持续出现429、5xx、超时或连接重置。
- 恢复阶段P95不高于T0基线P95的1.5倍，或满足项目既定SLA。
- 平台CPU、内存、线程池、连接池和队列回落。
- 供应商上游排队和错误率回落。

如果高压停止后仍持续异常，应记录“高压后未及时恢复”，这本身就是重要性能缺陷。

## 11. JMeter测试计划组件树

建议建立一个参数化JMX，所有梯度复用同一份文件：

```text
测试计划：KIMI-K3-API性能压测
├─ 用户定义的变量
├─ HTTP请求默认值
├─ HTTP信息头管理器
├─ 线程组：KIMI-K3固定模型
│  ├─ HTTP请求：POST-chat-completions
│  ├─ 响应断言：HTTP-200
│  ├─ JSON JMESPath断言：回答非空
│  ├─ JSR223断言：业务结构和内容合理
│  └─ 常数定时器（首轮关闭，可选）
└─ 简单数据写入器
```

正式压测时不要启用：

- 查看结果树（View Results Tree）
- 聚合报告GUI监听器
- 图形结果（Graph Results）
- 响应数据完整写入JTL
- 请求Header完整写入JTL
- 响应Header完整写入JTL

这些组件会消耗大量内存，可能导致压测机成为瓶颈。

## 12. JMeter逐项配置

### 12.1 用户定义的变量

| 名称 | 值 |
|---|---|
| `PROTOCOL` | `http` |
| `HOST` | `198.51.100.10` |
| `PORT` | 留空或`80` |
| `API_KEY` | `${__P(apiKey,)}` |
| `MODEL` | `${__P(model,kimi-k3(压测))}` |
| `STREAM` | `${__P(stream,false)}` |
| `MAX_TOKENS` | `${__P(maxTokens,100)}` |

### 12.2 HTTP请求默认值

| 字段 | 配置 |
|---|---|
| 协议 | `${PROTOCOL}` |
| 服务器名称或IP | `${HOST}` |
| 端口号 | `${PORT}` |
| 连接超时 | `10000`毫秒 |
| 响应超时 | `120000`毫秒 |
| Implementation | `HttpClient4` |
| Retrieve All Embedded Resources | 不勾选 |

若T0证明正常响应可能超过120秒，可统一调整为180000毫秒，但五档之间不得随意变更超时。

### 12.3 HTTP信息头管理器

非流式：

| 名称 | 值 |
|---|---|
| `Authorization` | `Bearer ${API_KEY}` |
| `Content-Type` | `application/json` |
| `Accept` | `application/json` |

流式专项将`Accept`改为：

```text
text/event-stream
```

### 12.4 线程组

| 字段 | 值 |
|---|---|
| 线程数 | `${__P(threads,25)}` |
| Ramp-up时间 | `${__P(rampSeconds,30)}` |
| 循环次数 | `${__P(loops,10)}` |
| 延迟创建线程 | 勾选 |
| 调度器 | 不勾选，首轮按循环次数执行 |
| 采样器错误后的动作 | `Continue`，由停止条件人工控制 |

### 12.5 HTTP Request

| 字段 | 值 |
|---|---|
| 名称 | `POST-chat-completions-kimi-k3` |
| 方法 | `POST` |
| 路径 | `/v1/chat/completions` |
| Content encoding | `UTF-8` |
| Body Data | 使用下方JSON |

```json
{
  "model": "${MODEL}",
  "messages": [
    {
      "role": "user",
      "content": "给出一套成都旅游一日游方案，50字左右"
    }
  ],
  "temperature": 0.2,
  "max_tokens": ${MAX_TOKENS},
  "stream": ${STREAM}
}
```

### 12.6 HTTP状态码断言

添加Response Assertion：

```text
Field to Test：Response Code
Pattern Matching Rules：Equals
Patterns to Test：200
```

### 12.7 非流式JSON断言

至少验证：

```text
$.choices[0].message.content 存在且非空
```

如响应包含标准字段，再验证：

```text
$.choices[0].finish_reason 存在
$.usage.total_tokens > 0
```

内容不要断言必须逐字包含某段固定答案。模型回答具有随机性，过严文本匹配会制造假失败。建议只验证：

- 回答非空。
- 回答字符数在20至300之间。
- 响应不存在顶层`error`。
- 业务结果可解析。

### 12.8 失败分类

建议通过JSR223 Assertion或后处理器给失败样本分类：

```text
AUTH_401：API Key无效或失效
FORBIDDEN_403：模型权限或租户权限不足
RATE_LIMIT_429：平台或供应商限流
PLATFORM_5XX：平台内部错误
UPSTREAM_5XX：供应商或上游模型错误
CONNECT_TIMEOUT：连接建立超时
READ_TIMEOUT：等待模型响应超时
CONNECTION_RESET：连接被重置
INVALID_JSON：非流式响应格式错误
EMPTY_CONTENT：HTTP成功但回答为空
SSE_INCOMPLETE：流式响应未正常结束
```

## 13. 正式执行命令

假设JMX位于：

```text
C:\Users\ZhuanZ1\Desktop\压测报告\JMeter脚本\KIMI-K3-API性能压测.jmx
```

先建立输出目录，但每次HTML报告目录必须不存在或为空。正式运行时关闭JMeter GUI，在PowerShell执行。

通用格式：

```powershell
$env:HEAP = "-Xms1g -Xmx2g -XX:MaxMetaspaceSize=256m"

& "C:\Users\ZhuanZ1\apache-jmeter-5.6.3\bin\jmeter.bat" `
  -n `
  -t "C:\Users\ZhuanZ1\Desktop\压测报告\JMeter脚本\KIMI-K3-API性能压测.jmx" `
  -l "C:\Users\ZhuanZ1\Desktop\压测报告\results\P1-25x20.jtl" `
  -e `
  -o "C:\Users\ZhuanZ1\Desktop\压测报告\reports\P1-25x20" `
  -Jthreads=25 `
  -Jloops=20 `
  -JrampSeconds=30 `
  -Jmodel="kimi-k3(压测)" `
  -Jstream=false `
  -JmaxTokens=100 `
  -JapiKey="sk-REPLACE_WITH_YOUR_KEY"
```

五档参数对应关系：

| 阶段 | `-Jthreads` | `-Jloops` | `-JrampSeconds` | JTL名称 | HTML目录名 |
|---|---:|---:|---:|---|---|
| P1 | 25 | 20 | 30 | `P1-25x20.jtl` | `P1-25x20` |
| P2 | 50 | 20 | 60 | `P2-50x20.jtl` | `P2-50x20` |
| P3 | 100 | 20 | 120 | `P3-100x20.jtl` | `P3-100x20` |
| P4 | 200 | 30 | 180 | `P4-200x30.jtl` | `P4-200x30` |
| P5 | 400 | 50 | 300 | `P5-400x50.jtl` | `P5-400x50` |

同一轮内的流式三档参数：

| 阶段 | `-Jthreads` | `-Jloops` | `-JrampSeconds` | `-Jstream` | JTL名称 | HTML目录名 |
|---|---:|---:|---:|---|---|---|
| S1 | 25 | 10 | 30 | `true` | `S1-stream-25x10.jtl` | `S1-stream-25x10` |
| S2 | 200 | 10 | 180 | `true` | `S2-stream-200x10.jtl` | `S2-stream-200x10` |
| S3 | 400 | 10 | 300 | `true` | `S3-stream-400x10.jtl` | `S3-stream-400x10` |

每次只替换表中的线程数、循环数、Ramp-up、JTL名称和HTML目录名，不得修改模型、问题、输出限制、超时或断言。

## 14. JTL保存原则

正式JTL只保留分析性能所需字段：

- 时间戳
- Elapsed
- Label
- Response Code
- Success
- Failure Message
- Bytes / Sent Bytes
- Latency
- Connect Time
- Thread Name

不保存：

- 完整请求Body
- 完整模型回答
- API Key
- 请求Header
- 响应Header
- 成功样本Response Data

否则最高档20,000次非流式模型回答和4,000次流式回答可能使JTL迅速膨胀，并导致JMeter内存或磁盘先成为瓶颈。

## 15. 每级升压条件

只有当前阶段同时满足下列条件，才进入下一档：

| 指标 | 建议条件 |
|---|---|
| 业务成功率 | `>= 99%` |
| HTTP 5xx | `< 1%` |
| HTTP 429 | `< 1%`，若平台另有明确限流SLA则按SLA |
| 连接超时/重置 | 无持续增长 |
| JMeter CPU | 建议峰值`< 80%` |
| 压测机内存 | 不持续下降到危险水平，不发生频繁Full GC |
| 平台CPU/内存 | 无持续饱和或失控增长 |
| 平台连接池/线程池 | 无持续耗尽 |
| 供应商调用 | 无明显连续限流、排队或失败 |

以上门槛是尚无正式SLA时的首轮建议，不应冒充产品最终验收标准。正式SLA应由产品、开发和运维共同确认。

## 16. 停止条件

任一条件满足时立即停止继续升压：

1. 当前阶段5xx错误率达到或超过2%。
2. 429达到或超过5%，并且已确认不是预期限流策略。
3. 连续出现连接重置、连接超时或读取超时。
4. P95达到基线P95的3倍以上并持续恶化。
5. 吞吐量不再增长，但并发、排队和响应时间继续增长。
6. 平台CPU、内存、线程池或连接池持续超过85%。
7. 数据库连接池耗尽，计费、消费记录或日志队列持续积压。
8. 供应商持续返回429、5xx或大量主动断流。
9. JMeter压测机CPU持续超过85%。
10. JMeter出现`OutOfMemoryError`、频繁Full GC或可用内存过低。
11. 发现影响环境稳定或其他测试人员工作的异常。

停止后应保存当前JTL、HTML报告、平台监控截图和服务日志时间区间，不应直接重跑覆盖现场。

## 17. 必须采集的JMeter指标

每个梯度单独记录：

- Samples
- 成功数和失败数
- 成功率和错误率
- Throughput，单位requests/s
- Average
- Median / P50
- P90
- P95
- P99
- Min / Max
- Connect Time
- Latency
- Received KB/sec
- Sent KB/sec
- 401、403、429、5xx数量
- 连接超时数量
- 读取超时数量
- Connection Reset数量
- 空回答数量
- 非法JSON数量
- Token使用量，如响应返回`usage`

注意：JMeter的`Latency`不得直接命名为“首Token时间”。

## 18. 平台侧监控

为了判断平台是否是瓶颈，测试期间至少每10至15秒采集：

- API网关CPU、内存、负载。
- 活动线程数、线程池排队和拒绝数。
- 当前连接数、连接建立速率和连接关闭状态。
- 上游HTTP连接池总数、活动数、空闲数、等待数。
- API Key鉴权耗时。
- 租户、模型权限和额度校验耗时。
- 模型路由耗时。
- 平台请求排队时间。
- 平台到供应商的请求耗时。
- 数据库CPU、连接池和慢SQL。
- 消费记录、计费记录和调用日志写入耗时。
- 日志或消息队列积压。
- 平台401、403、429、5xx及其错误码分类。
- JVM堆、非堆、GC暂停和Full GC，如平台为Java服务。

## 19. KIMI K3供应商侧监控

如果平台或供应商能够提供指标，至少采集：

- 上游请求数与并发请求数。
- 供应商请求排队时间。
- 供应商首Token时间TTFT。
- 完整生成时间。
- 输入Token、输出Token和总Token。
- 输出Token速度，tokens/s。
- 供应商429数量和限流原因。
- 供应商5xx数量和具体错误类型。
- 上游连接超时和读取超时。
- 上游主动断流和连接重置。
- 供应商并发、RPM和TPM限制。
- 若为自建推理服务：GPU利用率、显存、KV Cache、批处理大小和推理队列。

## 20. 平台与供应商瓶颈归因

建议平台日志为每个请求记录以下时间点：

```text
request_start
auth_end
route_end
upstream_start
upstream_first_token
upstream_end
record_end
request_end
```

可以计算：

```text
鉴权耗时 = auth_end - request_start
路由及平台前置耗时 = upstream_start - auth_end
供应商首Token时间 = upstream_first_token - upstream_start
供应商完整生成时间 = upstream_end - upstream_start
平台计费/日志后置耗时 = request_end - upstream_end
端到端耗时 = request_end - request_start
```

典型判断：

| 现象 | 优先怀疑方向 |
|---|---|
| 平台CPU/线程池/连接池饱和，上游耗时稳定 | 平台瓶颈 |
| 平台前置耗时增长，上游尚未开始 | 平台鉴权、路由或排队瓶颈 |
| `upstream_start`之后耗时暴涨，供应商429/5xx增加 | KIMI K3供应商瓶颈或限流 |
| 平台和供应商都正常，但JMeter CPU/GC异常 | 压测机瓶颈，结果无效 |
| 吞吐不增、响应时间增、供应商排队增 | 供应商达到容量拐点 |
| 模型返回已完成，但日志/计费写入积压 | 平台后置链路瓶颈 |

仅凭JMeter端到端响应时间不能直接认定“供应商性能不足”，必须结合上游调用指标或供应商直连对照。

## 21. 供应商直连对照测试

如果后续需要把平台开销和供应商开销严格拆开，需要开发或运维另行提供：

- KIMI K3供应商真实API地址。
- 供应商专用测试Key。
- 供应商模型精确名称。
- 供应商并发、RPM、TPM限制。
- 是否允许400并发直压。

然后使用相同问题、相同参数和相同梯度分别执行：

```text
A：JMeter -> AI 运营平台 -> KIMI K3供应商
B：JMeter -> KIMI K3供应商
```

近似计算：

```text
平台附加开销 = A端到端耗时 - B供应商直连耗时
```

当前API Key属于平台，因此当前方案测得的是“平台加KIMI K3供应商”的完整端到端性能，不是绕过平台后的纯供应商性能。

## 22. 结果目录

建议每档独立保存：

```text
C:\Users\ZhuanZ1\Desktop\压测报告\
├─ results\
│  ├─ T0-baseline.jtl
│  ├─ P1-25x20.jtl
│  ├─ P2-50x20.jtl
│  ├─ P3-100x20.jtl
│  ├─ P4-200x30.jtl
│  ├─ P5-400x50.jtl
│  ├─ S1-stream-25x10.jtl
│  ├─ S2-stream-200x10.jtl
│  ├─ S3-stream-400x10.jtl
│  └─ T3-recovery.jtl
├─ reports\
│  ├─ T0-baseline\
│  ├─ P1-25x20\
│  ├─ P2-50x20\
│  ├─ P3-100x20\
│  ├─ P4-200x30\
│  ├─ P5-400x50\
│  ├─ S1-stream-25x10\
│  ├─ S2-stream-200x10\
│  ├─ S3-stream-400x10\
│  └─ T3-recovery\
├─ server-monitoring\
├─ test-notes\
└─ KIMI-K3性能压测报告-2026-09-11.md
```

最终Markdown测试报告固定输出到：

```text
C:\Users\ZhuanZ1\Desktop\压测报告\KIMI-K3性能压测报告-2026-09-11.md
```

各阶段JMeter HTML报告也全部位于桌面`KIMI压测结果\reports`目录下。即使某阶段触发停止条件，仍须基于已经完成的阶段生成一份“部分执行报告”，不得丢失已获得的数据。

## 23. 最终结果汇总表

### 23.1 非流式容量结果

| 阶段 | 并发 | 样本数 | 成功率 | RPS | Avg(ms) | P90(ms) | P95(ms) | P99(ms) | Max(ms) | 429 | 5xx | 超时 | 重置 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| T0 | 1 |  |  |  |  |  |  |  |  |  |  |  |  |
| P1 | 25 |  |  |  |  |  |  |  |  |  |  |  |  |
| P2 | 50 |  |  |  |  |  |  |  |  |  |  |  |  |
| P3 | 100 |  |  |  |  |  |  |  |  |  |  |  |  |
| P4 | 200 |  |  |  |  |  |  |  |  |  |  |  |  |
| P5 | 400 |  |  |  |  |  |  |  |  |  |  |  |  |

### 23.2 流式连接结果

| 阶段 | 并发 | 样本数 | 成功率 | 完整流P95 | TTFT P95（服务端） | 未收到DONE | 中途断流 | 429 | 5xx |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| S1 | 25 |  |  |  |  |  |  |  |  |
| S2 | 200 |  |  |  |  |  |  |  |  |
| S3 | 400 |  |  |  |  |  |  |  |  |

### 23.3 资源与归因结果

| 并发 | 平台CPU峰值 | 平台内存峰值 | 连接池峰值 | 数据库连接池 | 供应商排队 | 供应商429 | 供应商5xx | 判断 |
|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 25 |  |  |  |  |  |  |  |  |
| 50 |  |  |  |  |  |  |  |  |
| 100 |  |  |  |  |  |  |  |  |
| 200 |  |  |  |  |  |  |  |  |
| 400 |  |  |  |  |  |  |  |  |

## 24. 最终报告必须给出的结论

1. `KIMI K3(压测)`端到端最高稳定并发是多少。
2. 25、50、100、200、400各档的成功率、RPS、P95和P99。
3. 性能拐点首次出现在哪一档。
4. 首次出现429、5xx、超时或断流的是哪一档。
5. 400并发是否通过；未通过时的主要失败类型。
6. 最大吞吐量是多少，以及吞吐量从哪一档开始不再增长。
7. 高压结束后是否能恢复到基线。
8. 瓶颈更可能位于平台、KIMI K3供应商还是压测机，并列出证据。
9. 平台鉴权、路由、连接池、计费和日志链路是否出现积压。
10. 后续建议：根据本轮结果决定是否需要专项定位、固定时长稳态测试或供应商直连对照；本方案本身不自动执行第二轮。

## 25. 执行检查清单

### 压测前

- [ ] 已确认这是允许压测的测试环境。
- [ ] 已通知开发、运维和供应商相关人员测试时间。
- [ ] 已确认唯一模型为`KIMI K3(压测)`。
- [ ] JMeter实际`model`值为`kimi-k3(压测)`。
- [ ] 已确认API Key状态、额度、RPM、TPM及并发限制。
- [ ] 已确认平台和供应商监控可用。
- [ ] 已记录服务器初始CPU、内存、连接池和队列状态。
- [ ] 已关闭JMeter查看结果树等GUI监听器。
- [ ] 已确保压测机可用内存大于约5 GB。
- [ ] 已确认本次只执行一轮完整流程，总正式压力请求35,750次。
- [ ] 已使用非GUI命令执行。
- [ ] 已完成1线程3次T0预检。
- [ ] 已确认JSON断言和错误分类生效。

### 压测中

- [ ] 每档使用独立JTL和HTML报告目录。
- [ ] 每档之间等待3至5分钟。
- [ ] 同步观察JMeter、平台和供应商资源。
- [ ] 按401、403、429、平台5xx、上游5xx、超时和重置分类错误。
- [ ] 达到停止条件时立即停止继续升压。
- [ ] 记录每档准确开始和结束时间。

### 压测后

- [ ] 完成1线程10次恢复验证。
- [ ] 保留JTL、HTML报告和服务端监控证据。
- [ ] 核对消费记录、计费记录和调用日志数量。
- [ ] 填写非流式、流式和资源归因汇总表，并输出到桌面指定报告路径。
- [ ] 输出最高稳定并发、拐点、错误点和恢复能力结论。
- [ ] 没有服务端证据时，不武断认定供应商是瓶颈。

## 26. 当前状态说明

截至本文档生成时，已完成以下只读核实：

- 平台账号登录成功。
- API Key可成功访问`GET /v1/models`。
- 目标模型精确API值为`kimi-k3(压测)`。
- 平台当前文档确认支持OpenAI兼容的`POST /v1/chat/completions`。
- 平台模型体验前端确认使用流式聊天接口。

尚未执行：

- 尚未向模型发送“成都一日游”问题。
- 尚未验证聊天请求Body的单次成功响应。
- 尚未创建或执行JMeter JMX。
- 尚未执行任何并发、循环或梯度压测。

下一步实施顺序应为：先生成JMX并用1线程3次验证，核对返回结构和断言；释放本机内存至至少5 GB后，在明确开始压测时执行一次完整流程：非流式25、50、100、200、400五档，流式25、200、400三档，最后完成恢复验证并将Markdown与HTML报告输出到桌面`KIMI压测结果`目录。

