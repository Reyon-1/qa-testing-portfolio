# AI 大模型 API 性能压测

对 **AI 运营平台** 的 `/v1/chat/completions` 接口做端到端性能压测，
覆盖两个大模型：**KIMI K3** 与 **GLM 5.2**。

压测目标是找出容量拐点，而不是跑出一个好看的数字——因此每个阶段都设置了
**升级闸门**（失败率、窗口失败率、恢复阈值），一旦触发就优雅停止，
不继续制造无效请求。

---

## 一、核心结论

### KIMI K3

| 阶段 | 并发 | 实际样本 | 成功/失败 | 成功率 | 有效 RPS | P95(ms) | P99(ms) |
|---|---:|---:|---:|---:|---:|---:|---:|
| T0 基线 | 1 | 3 | 3/0 | 100% | 0.044 | 28,931 | 28,931 |
| P1 | 25 | 500 | 499/1 | 99.8% | 1.278 | 23,756 | 31,213 |
| P2 | 50 | 1,000 | 1,000/0 | **100%** | **1.781** | 33,080 | 39,434 |
| P3 | 100 | 1,297 | 920/377 | 70.93% | 1.499 | 58,630 | 67,156 |
| T3 恢复 | 1 | 10 | 10/0 | 100% | 0.055 | 37,291 | 37,291 |

**容量拐点在 50～100 线程之间。** 50 线程 1,000 请求成功率 100%；
升到 100 线程后出现 377 个 HTTP 502（失败率 29.07%），
有效成功吞吐反而从 1.781 RPS 掉到 1.499 RPS。

关键观察：502 高度集中在 **30～32 秒**，呈现明显固定超时特征，
而非随机崩溃；错误连续覆盖约 7 分 35 秒。

### GLM 5.2

| 阶段 | 并发 | 实际样本 | 成功/失败 | 成功率 |
|---|---:|---:|---:|---:|
| P1 | 25 | 500 | 490/10 | 98.0% |
| P2 | 50 | 1,000 | 982/18 | 98.2% |
| P3（旧模型名） | 100 | 1,036 | 866/170 | 83.59% |
| P3（当前模型名） | 100 | 1,512 | 1,256/256 | 83.07% |
| T3 恢复 | 1 | 10 | 0/10 | 账户阻塞 |

**100 线程稳定复现容量失稳**，用新旧两个模型名称各验证一次，
失败率分别为 16.41% 和 16.93%，HTTP 502 率 15.25% / 15.28%，
说明不是命名问题而是真实容量瓶颈。

值得注意：T3 恢复阶段 10 次全部返回 `overdue-payment`（账户欠费），
属于**账户状态阻塞**而非技术恢复失败，因此该轮恢复能力**无法判定**——
报告中如实记录了这一点，没有粉饰成"通过"。

### GLM 5.2 流式（SSE）专项

同一模型的**流式**调用单独做了一轮，50 / 100 / 200 三档并发：

| 阶段 | 并发 | 样本 | 成功率 | RPS | P50(ms) | P95(ms) |
|---|---:|---:|---:|---:|---:|---:|
| S1 | 50 | 1,000 | 99.70% | 1.4875 | 14,080 | 21,987 |
| S2 | 100 | 1,600 | 98.94% | 2.2514 | 25,513 | 45,917 |
| S3 | 200 | 2,600 | 98.04% | 2.2585 | 49,103 | 91,551 |

**并发 100 → 200，吞吐几乎不再增长（+0.32%），P95 却接近翻倍**，
属于明显退化区。容量拐点同样落在 **50～100 并发之间**。
高压后恢复验证 10/10 通过。

> 流式用例定义了 **7 条严格 SSE 断言**（`Content-Type`、事件 JSON 合法性、
> `[DONE]` 唯一且位于末尾、拼接正文非空等）。
> 结果中发现 **6 个「HTTP 200 但正文为空」**的响应被断言正确拦下——
> 普通 JMeter 采样器会把它计为成功，这是流式测试里典型的"假成功"。

### 官网直连 vs 平台链路对比

用同一模型家族做了官网直连与平台链路的对照（50 线程流式）：

| 指标 | 官网直连 | 平台链路 | 差异 |
|---|---:|---:|---:|
| 样本数 | 250 | 1,000 | 样本量不同 |
| 成功率 | 100% | 99.7% | 官网高 0.3pp |
| P50 | 10,646 ms | 14,080 ms | 官网低约 24% |
| P95 | 15,439 ms | 21,987 ms | 官网低约 30% |
| P99 | 18,020 ms | 27,910 ms | 官网低约 35% |

> 报告如实标注了**局限性**：官网仅 250 次、平台 1,000 次，
> 样本量与时长的差异使官网更不容易暴露长尾，
> 因此**不能视为严格等量 A/B 实验**，也未将差异单方归因于平台。

---

## 二、目录结构

```
performance-testing/
├─ 压测总结.html                      ← 双模型汇总单页（自包含，浏览器直接打开）
├─ 压测方案/
│  └─ Token平台-Key接入-Agent场景压测方案-v2.md
├─ 官网对比压测/                       ← 官网直连 vs 平台链路对照
│  ├─ 官网GLM-5.2流式压测及平台对比报告-2026-09-15.md
│  └─ JMeter脚本/
├─ KIMI K3 压测/
│  ├─ KIMI-K3性能压测报告.md          ← 完整结论报告（核心交付物）
│  ├─ KIMI-K3性能压测报告.html        ← 同内容的独立 HTML 版（含内联样式，可直接打开）
│  ├─ 压测方案/                       ← 测试方案与报告模板
│  ├─ JMeter脚本/                     ← .jmx 测试计划 + .ps1 执行脚本
│  └─ 原始结果/                       ← 阶段汇总 CSV、每阶段 summary JSON
└─ GLM 5.2 压测/
   ├─ GLM-5.2性能压测报告.md           ← 非流式完整结论
   ├─ GLM-5.2流式压测专项报告-2026-09-15.md  ← 流式 SSE 专项
   └─ （其余同上结构）
```

**建议先读**：`压测总结.html`（一页看完全部结论），
或 `GLM-5.2流式压测专项报告`（流式测试与 SSE 断言设计）。

> **关于 压测总结.html**：把两份压测报告的结论、对比与关键证据合并成的单页，
> CSS 内联、无外部依赖，下载后用浏览器打开即可。GitHub 不渲染 HTML，
> 点进去看到的是源码，**需要下载到本地打开**。

> **关于 JMeter Dashboard**：原压测每轮还生成了 JMeter HTML Dashboard。
> 由于这些目录 88% 是 JMeter 官方模板的重复静态资源（jQuery / Bootstrap / flot 等
> 共 50 MB、2200 余个文件），且 GitHub 不渲染 HTML，
> 已将整目录移出仓库。**报告结论与原始数据不受影响**，
> `原始结果/` 下保留了每阶段的 summary JSON 与阶段汇总 CSV。
> 如需可交互的 Dashboard，按下方复现方式重跑一次压测即可重新生成完整目录。

---

## 三、技术栈与方法

| 维度 | 做法 |
|---|---|
| 压测工具 | Apache JMeter 5.6.3，非 GUI 模式 |
| 并发模型 | 闭环并发（线程组 × 循环次数） |
| 执行编排 | PowerShell 脚本驱动，支持 `-StartAt` 断点续测 |
| 结果采集 | JTL 原始样本 + summary JSON + 阶段汇总 CSV |
| 报告生成 | JMeter Dashboard + 自动生成 Markdown 报告 |
| 环境监控 | 本机资源采样（CPU/内存）随压测同步记录 |

**闸门设计**：每阶段结束后校验失败率与窗口失败率，
超过阈值则触发紧急闸门优雅停止，避免污染被测环境。

---

## 四、复现方式

需要 JMeter 5.6.3+ 与一个可用的模型 API Key：

```powershell
# 1. 修改执行脚本里的地址与密钥（不要提交真实密钥）
#    Accept / Model / ApiKey 通过 -J 参数注入
$env:API_KEY = '<你的密钥>'

# 2. 执行压测（支持断点续测）
cd "KIMI K3 压测\JMeter脚本"
.\执行KIMI-K3压测.ps1

# 3. 结果输出到 原始结果\（同时会生成 JMeter Dashboard 目录）
```

`-J` 参数：`host`、`model`、`stream`、`accept`、`apiKey`。

---

## 五、免责与脱敏说明

- 本仓库为**个人测试作品**，用于展示性能测试方法与报告能力。
- 所有环境地址、API 密钥已脱敏为占位符（`198.51.100.x` / `sk-REPLACE_WITH_YOUR_KEY`）。
- 报告中保留的是**方法与结论**，不含任何真实凭证。
- 压测结果受上游供应商、网络与账户状态影响，**仅代表本次测试时刻**，
  不能直接外推为长期容量承诺。
